## What changed

Summarize the changes in this PR. Include the module(s) touched and how the work fits the SRS requirements.

## Related issue

- Closes #<issue-number>

## SRS section reference

Point to the exact section(s) of `docs/SRS.docx` / `docs/Stock_Management_Workflow.pdf` that this PR implements (e.g. "SRS section 3.1 Stock Receiving Management", "Workflow steps 5–6"). If a requirement was ambiguous, note the ambiguity here and how you resolved it.

## How it was tested

- [ ] Server: `npm run lint`, `npm run typecheck`, `npm test`
- [ ] Client: `npm run lint`, `npm run format:check`, `npm run build`
- [ ] Integration test(s) added/updated for the module in `server/tests/integration/`
- [ ] Manual verification steps (describe how to reproduce)
